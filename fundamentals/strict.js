'use strict';

let hasDriverLicense = false;
const passTime = true;

if (passTime) hasDriversLicense = true;
if (hasDriverLicense) console.log("I can drive now!");


//'interface' is a reserved word in strict mode and will cause a syntax error
let interface = "check Interface";
console.log(interface);

//'private' is a reserved word in strict mode and will cause a syntax error
let private = "check private";
console.log(private); 